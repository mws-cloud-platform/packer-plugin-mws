#cloud-config
packages:
    - nginx
users:
    - groups: docker
      name: custom
      shell: /bin/zsh
    - groups: sudo
      name: packer
      shell: /bin/bash
      ssh-authorized-keys:
        - key
      sudo: ALL=(ALL) NOPASSWD:ALL
