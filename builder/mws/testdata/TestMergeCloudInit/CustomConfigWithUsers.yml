#cloud-config
packages:
    - nginx
users:
    - groups: sudo
      name: testuser
      shell: /bin/bash
      ssh-authorized-keys:
        - testkey
      sudo: ALL=(ALL) NOPASSWD:ALL
    - groups: docker
      name: customuser
      shell: /bin/zsh
