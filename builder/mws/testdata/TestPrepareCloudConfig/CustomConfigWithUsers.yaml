#cloud-config
packages:
    - nginx
users:
    - groups: sudo
      name: packer
      shell: /bin/bash
      ssh-authorized-keys:
        - key
      sudo: ALL=(ALL) NOPASSWD:ALL
    - groups: docker
      name: custom
      shell: /bin/zsh
