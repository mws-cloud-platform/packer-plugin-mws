#cloud-config
packages:
    - nginx
runcmd:
    - echo "Hello World"
users:
    - groups: sudo
      name: packer
      shell: /bin/bash
      ssh-authorized-keys:
        - key
      sudo: ALL=(ALL) NOPASSWD:ALL
